[package]
name = "tapecpy"
version = "0.1.0"
edition = "2024"
description = "Linux-native LTFS tape tool (LTFSCopyTUI)"
license = "MIT OR Apache-2.0"

[dependencies]
libc = "0.2"
quick-xml = "0.41"

[profile.release]
lto = true
strip = true
